{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "source": "index.html",
  "scripts": {
    "prepareauth": "mkdir -p dist/auth && cp ../dist/worker.js ./dist/auth/ && cp -r ../dist/assets ./dist/auth",
    "start": "rm -rf dist && mkdir dist && npm run prepareauth && parcel",
    "build": "rm -rf dist && parcel build --dist-dir dist && npm run prepareauth"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "@hiveio/hb-auth": "file:../"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.16.3",
    "@parcel/transformer-raw": "^2.16.3",
    "crypto-browserify": "^3.12.0",
    "events": "^3.1.0",
    "parcel": "^2.10.2",
    "path-browserify": "^1.0.0",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0",
    "string_decoder": "^1.3.0",
    "vm-browserify": "^1.1.2"
  }
}
