{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "source": "index.html",
  "scripts": {
    "start": "rm -rf dist && mkdir dist && mkdir dist/auth && cp ../dist/worker.js ./dist/auth/ && parcel",
    "build": "parcel build --dist-dir dist"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "@hiveio/hb-auth": "file:../"
  },
  "devDependencies": {
    "crypto-browserify": "^3.12.0",
    "events": "^3.1.0",
    "parcel": "^2.10.2",
    "path-browserify": "^1.0.0",
    "process": "^0.11.10",
    "stream-browserify": "^3.0.0",
    "string_decoder": "^1.3.0",
    "vm-browserify": "^1.1.2"
  }
}
